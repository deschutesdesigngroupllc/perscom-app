---
paths:
  - 'app/Actions/**'
---

# Actions

## Actions have one static handle()
Give each Action a single public static handle() entry point. Keep multi-method domain logic in app/Services instead.
