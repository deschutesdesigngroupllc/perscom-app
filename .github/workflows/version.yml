name: Update Version

on:
  workflow_call:
    outputs:
      new_tag:
        description: "The application tag"
        value: ${{ jobs.update-version.outputs.new_tag }}
      changelog:
        description: "The application changelog"
        value: ${{ jobs.update-version.outputs.changelog }}

jobs:
  update-version:
    name: Update Application Version
    runs-on: ubuntu-latest
    outputs:
      new_tag: ${{ steps.tag_version.outputs.new_tag }}
      changelog: ${{ steps.tag_version.outputs.changelog }}
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v3

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.FORGE_SSH_PRIVATE_KEY }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.1
          extensions: dom, curl, libxml, mbstring, zip, pcntl, pdo, sqlite, pdo_sqlite
          coverage: none
          tools: composer

      - name: Require Forge CLI
        run: composer global require laravel/forge-cli

      - name: Bump Application Version and Tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          default_bump: patch
          default_prerelease_bump: prerelease
          release_branches: master
          pre_release_branches: staging
          append_to_pre_release_tag: prerelease

      - name: Update Application Version for Staging
        if: github.ref == 'refs/heads/staging'
        run: |
          forge server:switch ${{ secrets.FORGE_SERVER_NAME }}
          forge command ${{ secrets.FORGE_STAGING_SERVER_SITE }} --command="php current/artisan env:set APP_VERSION ${{ steps.tag_version.outputs.new_tag }}"
        env:
          FORGE_API_TOKEN: ${{ secrets.FORGE_API_TOKEN }}

      - name: Update Application Version for Production
        if: github.ref == 'refs/heads/master'
        run: |
          forge server:switch ${{ secrets.FORGE_SERVER_NAME }}
          forge command ${{ secrets.FORGE_PRODUCTION_SERVER_SITE }} --command="php current/artisan env:set APP_VERSION ${{ steps.tag_version.outputs.new_tag }}"
        env:
          FORGE_API_TOKEN: ${{ secrets.FORGE_API_TOKEN }}
